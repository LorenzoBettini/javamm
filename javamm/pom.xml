<?xml version="1.0" encoding="UTF-8"?>
<project>
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<relativePath>../javamm.releng/pom.xml</relativePath>
		<groupId>javamm</groupId>
		<artifactId>javamm.releng</artifactId>
		<version>1.0.0-SNAPSHOT</version>
	</parent>

	<artifactId>javamm</artifactId>
	<packaging>eclipse-plugin</packaging>

	<build>
		<plugins>
			<!--
				This is to get the path of the Xbase source jar dependency absolute path
				See http://stackoverflow.com/questions/2359872/can-i-use-the-path-to-a-maven-dependency-as-a-property/6934552#6934552
			-->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>
				<version>2.10</version>
				<executions>
					<execution>
						<goals>
							<goal>properties</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-clean-plugin</artifactId>
				<version>2.5</version>
				<configuration>
					<skip>${mwe2.skip}</skip>
					<filesets>
						<fileset>
							<directory>${basedir}/src-gen</directory>
						</fileset>
						<fileset>
							<directory>${basedir}/xtend-gen</directory>
						</fileset>
						<!-- clean ui plugin as well -->
						<fileset>
							<directory>${basedir}/../${project.artifactId}.ui/src-gen</directory>
						</fileset>
						<fileset>
							<directory>${basedir}/../${project.artifactId}.ui/xtend-gen</directory>
						</fileset>
						<!-- clean test fragment as well -->
						<fileset>
							<directory>${basedir}/../${project.artifactId}.tests/src-gen</directory>
						</fileset>
						<fileset>
							<directory>${basedir}/../${project.artifactId}.tests/xtend-gen</directory>
						</fileset>
						<!-- clean swtbot.test and ide target as well, to avoid warnings, since
						that contain installed artifacts -->
						<fileset>
							<directory>${basedir}/../${project.artifactId}.swtbot.tests/target</directory>
						</fileset>
						<fileset>
							<directory>${basedir}/../${project.artifactId}.ide.site/target</directory>
						</fileset>
					</filesets>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>exec-maven-plugin</artifactId>
				<version>1.2.1</version>
				<executions>

					<execution>
						<id>show-properties</id>
						<phase>generate-sources</phase>
						<goals>
							<goal>exec</goal>
						</goals>
						<configuration>
							<executable>echo</executable>
							<arguments>
								<argument>-e</argument>
								<argument>path to xbase source jar=\n</argument>
								<argument>${org.eclipse.xtext:org.eclipse.xtext.xbase:jar:sources}\n</argument>
								<argument>path to common types source jar=\n</argument>
								<argument>${org.eclipse.xtext:org.eclipse.xtext.common.types:jar:sources}\n</argument>
							</arguments>
						</configuration>
					</execution>
				
					<execution>
						<id>mwe2Launcher</id>
						<phase>generate-sources</phase>
						<goals>
							<goal>java</goal>
						</goals>
						<configuration>
							<skip>${mwe2.skip}</skip>
							<includeProjectDependencies>false</includeProjectDependencies>
							<includePluginDependencies>true</includePluginDependencies>
							<mainClass>org.eclipse.emf.mwe2.launch.runtime.Mwe2Launcher</mainClass>
							<arguments>
								<argument>file://${project.basedir}/src/javamm/GenerateJavamm.mwe2</argument>
								<argument>
									-p
								</argument>
								<argument>
									runtimeProject=/${project.basedir}
								</argument>
								<argument>
									xbaseSources=archive:file:/${org.eclipse.xtext:org.eclipse.xtext.xbase:jar:sources}!
								</argument>
								<argument>
									commonTypesSources=archive:file:/${org.eclipse.xtext:org.eclipse.xtext.common.types:jar:sources}!
								</argument>
							</arguments>
						</configuration>
					</execution>

				</executions>
				
				<dependencies>
					<dependency>
						<groupId>org.eclipse.xtext</groupId>
						<artifactId>org.eclipse.xtext.xtext</artifactId>
						<version>${xtext-version}</version>
					</dependency>
					<dependency>
						<groupId>org.eclipse.xtext</groupId>
						<artifactId>org.eclipse.xtext.xbase</artifactId>
						<version>${xtext-version}</version>
					</dependency>
				</dependencies>
			</plugin>

			<plugin>
				<groupId>org.eclipse.xtend</groupId>
				<artifactId>xtend-maven-plugin</artifactId>
			</plugin>

			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>findbugs-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>
	
	<dependencies>
		<!-- We need to download these source jars that will be used during the
			mwe2 workflow to look for Xbase's ImplCustom classes -->
		<dependency>
			<groupId>org.eclipse.xtext</groupId>
			<artifactId>org.eclipse.xtext.xbase</artifactId>
			<version>${xtext-version}</version>
			<classifier>sources</classifier>
		</dependency>
		<dependency>
			<groupId>org.eclipse.xtext</groupId>
			<artifactId>org.eclipse.xtext.common.types</artifactId>
			<version>${xtext-version}</version>
			<classifier>sources</classifier>
		</dependency>
	</dependencies>
</project>
